export class Favs {
     cid:number;
	 username:string;
	 capital:string;               //Getting player id to be stored as private
	 common_name:string;        //Getting player country to be stored as private
	 region:string;           //Getting player name to be stored as private
	 officialname:string;       //Getting player fullName to be stored as private
     flag:string;     //Getting info about major teams to be stored as private
     population:number;     //Getting player currentAge to be stored as private
     coatOfArms:string;       //Getting player imageURL to be stored as private
	 area:number;		//Getting player role to be stored as private
	 status:boolean;
     constructor(){};
 }
 // Model class to store the favourites with the attributes