export class Recommended {
    pid: number;
    country: string;
    name: string;
    fullName: string;
    majorTeams: string;
    currentAge: string;
    imageURL: string;
    playingRole: string;
 }
 //Model class for recommended with all the properties
 //which are to be used and stored in the backend